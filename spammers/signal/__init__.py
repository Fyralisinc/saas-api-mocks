"""Signal mock — the signal-cli linked-device METHOD contract over a transport
substitution (HTTP reads + a WS receive gateway), carrying the REAL signal-cli
envelope shapes. See spammers/signal/app.py and migration 0025_signal.sql."""
